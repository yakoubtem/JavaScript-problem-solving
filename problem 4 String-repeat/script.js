// whrite a function called repeatStr which repeats the given string exactly n times

/*
function repeatStr(str,nbr) {
    let newstring ='';
    for (let i = 0; i < nbr; i++) {
        newstring += str;
    }
    return newstring;
}  */

function repeatStr(str,nbr) {
   return console.log(str.repeat(nbr));
}
repeatStr("yaakoub \n",5);

